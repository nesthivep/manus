[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Add markers
markers =
    asyncio: mark test as requiring asyncio
    unit: mark test as a unit test
    integration: mark test as an integration test

# Settings for asyncio tests
asyncio_mode = auto
